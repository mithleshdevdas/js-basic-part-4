const codinlag = ['js', 'py', 'c++', 'java']

codinlag.forEach((e) => {
    // console.log(e);
})
codinlag.forEach((elemet, index, arr) => {
    // console.log(`elemet is ${elemet} index number is ${index} arr is ${arr}`);
})

const mYarr = [
    {
        lag: 'js',
        lagname: 'javascript'
    },
    {
        lag: 'py',
        lagname: 'python'
    },
    {
        lag: 'jv',
        lagname: 'java'
    },
    
]
myarr.forEach((item) => {
    console.log(item.lagname);


})
// @ts-nocheck

// 
